import { ConfigInterface } from '../../base-types';
import { Session } from '../../session/session';
export interface MigrateToExpiringTokenParams {
    shop: string;
    nonExpiringOfflineAccessToken: string;
}
export type MigrateToExpiringToken = (params: MigrateToExpiringTokenParams) => Promise<{
    session: Session;
}>;
export declare function migrateToExpiringToken(config: ConfigInterface): MigrateToExpiringToken;
//# sourceMappingURL=migrate-to-expiring-token.d.ts.map