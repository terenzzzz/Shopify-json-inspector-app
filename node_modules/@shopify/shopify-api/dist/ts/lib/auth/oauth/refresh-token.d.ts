import { ConfigInterface } from '../../base-types';
import { Session } from '../../session/session';
export interface RefreshTokenParams {
    shop: string;
    refreshToken: string;
}
export type RefreshToken = (params: RefreshTokenParams) => Promise<{
    session: Session;
}>;
export declare function refreshToken(config: ConfigInterface): RefreshToken;
//# sourceMappingURL=refresh-token.d.ts.map