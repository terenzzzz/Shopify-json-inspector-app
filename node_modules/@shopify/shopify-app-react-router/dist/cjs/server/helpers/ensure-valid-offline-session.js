'use strict';

var createOrLoadOfflineSession = require('./create-or-load-offline-session.js');
var ensureOfflineTokenIsNotExpired = require('./ensure-offline-token-is-not-expired.js');

async function ensureValidOfflineSession(params, shop) {
    const session = await createOrLoadOfflineSession.createOrLoadOfflineSession(params, shop);
    if (!session)
        return undefined;
    return ensureOfflineTokenIsNotExpired.ensureOfflineTokenIsNotExpired(session, params, shop);
}

exports.ensureValidOfflineSession = ensureValidOfflineSession;
//# sourceMappingURL=ensure-valid-offline-session.js.map
